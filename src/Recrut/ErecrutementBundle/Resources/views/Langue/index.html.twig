{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Langue list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Langue</th>
                <th>Niveau</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('langue_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.langue }}</td>
                <td>{{ entity.niveau }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('langue_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('langue_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('langue_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
