{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Poster Annonce </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('poste') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
