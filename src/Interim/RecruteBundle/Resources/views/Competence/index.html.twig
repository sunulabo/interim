{% extends "InterimRecruteBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}

{% block interimrecrutebundle_body %}
    <h1>Competence list</h1>

  <table class="table table-striped">
        <thead>
            <tr>
                <th></th>
                <th>Competence</th>
                <th>Niveau</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('competence_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.competence }}</td>
                <td>{{ entity.niveau }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('competence_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('competence_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('competence_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
