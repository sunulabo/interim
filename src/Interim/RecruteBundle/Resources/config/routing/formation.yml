formation:
    path:     /
    defaults: { _controller: "InterimRecruteBundle:Formation:index" }

formation_show:
    path:     /{id}/show
    defaults: { _controller: "InterimRecruteBundle:Formation:show" }

formation_new:
    path:     /new
    defaults: { _controller: "InterimRecruteBundle:Formation:new" }

formation_create:
    path:     /create
    defaults: { _controller: "InterimRecruteBundle:Formation:create" }
    requirements: { _method: post }

formation_edit:
    path:     /{id}/edit
    defaults: { _controller: "InterimRecruteBundle:Formation:edit" }

formation_update:
    path:     /{id}/update
    defaults: { _controller: "InterimRecruteBundle:Formation:update" }
    requirements: { _method: post|put }

formation_delete:
    path:     /{id}/delete
    defaults: { _controller: "InterimRecruteBundle:Formation:delete" }
    requirements: { _method: post|delete }
